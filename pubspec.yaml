name: section_scrollbar
description: A section-aware custom scrollbar with a floating current-section label.
version: 0.1.1+1
repository: https://github.com/Ethiel97/section_scrollbar_flutter.git
screenshots:
  - description: Custom scrollbar with a floating current-section badge while scrolling.
    path: assets/demo.gif


environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0
